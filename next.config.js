// Ejemplo de configuración base para Webpack en Next.js
const path = require('path');

module.exports = {
  webpack: (config, { isServer }) => {
    // Personalizaciones aquí si es necesario
    return config;
  },
};
